Scoped.extend("module:AbstractConsumer", [
    "base:Class"
], function (Class, scoped) {
    return Class.extend({scoped: scoped}, function (inherited) {
        return {

            // TODO

        };
    });
});